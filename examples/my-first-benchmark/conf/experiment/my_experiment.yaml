# @package _global_
### Experiment setup for running 20 bootstraps, on 4 CPU's. Uploads results to wandb.

defaults:
  - override /resample: bootstrap
  - override /callbacks:
    - wandb

callbacks:
  wandb:
    project: my-first-benchmark

n_bootstraps: 20
n_jobs: 4