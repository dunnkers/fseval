hydra:
  help:
    app_name: fseval
    header: |
      fseval benchmarking library.

defaults:
  - base_config
  - dataset: iris
  - cv: train_test_split
  - resample: bootstrap
  # - estimator: chi2
  - pipeline: feature_ranking
  - callbacks:
    - stdout
    - wandb
  # - task: ${dataset.task}
  # - estimator@pipeline/ranker/estimator: ${dataset.task}/giezer
  # - pipeline/adapter: ${dataset.adapter}
  # - pipeline/ranker/estimator: 
# pipeline:
#   # adapter: ${dataset.adapter}
#   task: ${dataset.task}
  # - estimator@pipeline.ranker: chi2
# _target_: ${pipeline.target_class}
# pipeline:
#   dataset: ${defaults.datataset}
  # cv: ${defaults.cv}
  # resample: ${defaults.resample}
  # task: ${defaults.dataset.task}
wandb:
  project: toy-wandb-fs
# pipeline:
#   callbacks:
#     - wandb
#   - ranker: tabnet
#   - validator: dt
# ranker:
#   task: ${dataset.task}
# validator:
#   task: ${dataset.task}