project: toy-wandb-fs
defaults:
  - base_config
  - dataset: xor
  - cv: train_test_split
  - resample: bootstrap
  - ranker: tabnet
  - validator: dt
ranker:
  task: ${dataset.task}
validator:
  task: ${dataset.task}