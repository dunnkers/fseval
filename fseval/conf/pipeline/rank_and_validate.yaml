defaults:
  - base_rank_and_validate
  - resample: bootstrap
  - estimator@ranker: relieff
  - estimator@validator: dt
ranker:
  task: ${dataset.task}
validator:
  task: ${dataset.task}
n_bootstraps: 3
