defaults:
  - base_rank_and_validate
  - /resample: bootstrap
  - /estimator@ranker: anova_f_value
  - /estimator@validator: decision_tree
ranker:
  task: ${dataset.task}
validator:
  task: ${dataset.task}
n_bootstraps: 2
